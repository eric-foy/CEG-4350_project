dup2
copies the file pointer into a new variable.

creat
opens a file to bu used later.

pipe
opens two file descriptors, i.e. stdin, stdout, to be used for IPC.

fork
spawns a new child process with new memory space and duplicates the parent process into it.

pthread_create
new thread that runs the start routine passed in.

system
exec's a shell command

printf
produce formated output
